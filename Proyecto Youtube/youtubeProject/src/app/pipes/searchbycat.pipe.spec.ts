import { SearchbycatPipe } from './searchbycat.pipe';

describe('SearchbycatPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchbycatPipe();
    expect(pipe).toBeTruthy();
  });
});
