import { PrimosPipesPipe } from './primos-pipes.pipe';

describe('PrimosPipesPipe', () => {
  it('create an instance', () => {
    const pipe = new PrimosPipesPipe();
    expect(pipe).toBeTruthy();
  });
});
